Kodel kaip DB naudojam prisma? Kuo ji isskirtine?

1) Authentication hardening

Refresh token rotation + expiry handling (you’re basically there)

Account lockout / throttling (already added throttler; add per-route limits)

Email verification (optional but useful before 2FA)

2FA (TOTP / Google Authenticator recommended)

Session management UI

    “Active sessions/devices”

    “Logout all devices”

2) Authorization core (before building any business features)

RBAC model finalized

    global roles: ADMIN (system)

    project roles: OWNER/ADMIN, EDITOR, READER

Guards + decorators

    @ProjectRole('ADMIN') etc.

Admin panel foundation

    manage users, disable/enable users, reset 2FA, force logout, view audit logs

3) Project domain foundation

Project CRUD + membership management UI

    create project

    invite member (email)

    role assignment

Project lock function

    locked project blocks edits (except admins)

    lock reason + lockedBy + lockedAt

4) Audit logging everywhere (do early, so you don’t retrofit later)

Unified AuditLog service

    middleware/interceptor that logs mutations

    log: create/update/delete, comments, nodes, files, roles

    store actorId, projectId, entity, entityId, diff/details, timestamp

5) Project structure (Nodes) + components

Nodes tree (project structure)

    hierarchical nodes

    permissions checked at project level

Components attached to nodes

    tasks

    notes/comments

    links

    attachments/files

6) File upload + VirusTotal

File upload pipeline

    upload -> store -> scan -> mark safe/unsafe/quarantine

VirusTotal integration

    async scan job

    status polling

    block download until safe (or warn)

7) User account page

Account settings

    change password

    enable/disable 2FA

    view active sessions

    audit of own actions