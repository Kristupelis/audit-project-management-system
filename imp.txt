Prisijungimai:

test@example.com
Password123!

KristupasTest
ckristupas@gmail.com
26KarvesPienas!


Run backend:
cd apps/api
pnpm start:dev

http://localhost:4000

Run frontend:
cd apps/web
pnpm dev

http://localhost:3000

Edit DB:

pnpm prisma studio

Print tree:

git ls-files

Galimai nereikalingi:
src/app/api/auth/refresh/route.ts
session-expiry-prompt.tsx - nereikalingas 100 proc


Projekto uzrakinimas, Interaktyvus grafas, VirusTotal. Admin - redaguoti useriu duomenis, perziureti system logsus, direct permissions, projekto redagavimo mygtukas paciam projekte
Sutvarkyti, kad uzblokuotas naudotojas ar istrintas naudotojas turi buti iskart atjungtas, jei jo sesija yra aktyvi
Negaliu but prisijunges su dviem naudotojais vienu metu - naudot atskiras narsykles bruf


Jei "use client", tai:

const t = useT();
<h1>{t.projects.title}</h1>

Jei ne, tai:

import { cookies } from "next/headers";
import { getDictionary, type Locale } from "@/i18n/get-dictionary";

const cookieStore = await cookies();
const localeCookie = cookieStore.get("locale")?.value;
const locale: Locale = localeCookie === "lt" ? "lt" : "en";
const t = getDictionary(locale);


Okay, lets add very important features now. Write everything I need to change to make the features work
1. For account blocking and deleting - if an account gets blocked or deleted by admin, do an immediate log out for that account because now, If I am logged in with that account and it gets blocked or deleted, I can still use the app, go into projects, etc., this is not okay, this needs to be fixed
2. User data editing - an admin should be able to edit users data, like users can edit their own account data. An admin should be able to also change the user account password without needing to now the current one (I think that is logical).
3. Project locking - lets add a project locking attribute to a project model, so in this way, if a project owner locks the project, no one can edit, delete, create or change in the project (I think the best thing is just show that project is locked and dont let project members open the projects). Only a project owner could open and change anything in a locked project.
4. System logs - add a system logs page in an admin panel, where an admin would see all the system logs (not project logs), like falsely inputted passwords, unhandled errors, system errors and etc. Besides that, these errors need to be generated by the system, so add that.
5. Account blocking - automatically block an account when user inputs wrong password 6 times in a row in 5 minutes (it can then be unblocked only by an admin).
We can go through these features step by step, while testing them too or all at the same time, or grouped by similarity. However you want to do it. If you need any extra files provided from me, tell me.
